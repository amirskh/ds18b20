# ds18b20
reading data of the ds18b20 using STM32
in order to read data from the sensor add header file in Inc and source file in Src then use the temp_read() function
connect the vcc to vcc 
GND to GND pullup the data using 4.7k resistor 
connect the middle pin(data to PA4)
